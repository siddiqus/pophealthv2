<tr data-provider="<%= provider_row.id %>">
	<td><%= provider_row.family_name%></td>
	<td><%= provider_row.given_name%></td>
	<td><%= provider_row.team.try(:name) || "N/A" %></td>
	<% if current_user.admin? %>
		<td> <%= link_to "view", :controller => "providers", :action => "show", :id => "#{provider_row.id}" %>  | <%= link_to "manage patients", manage_provider_patients_path(provider_row)%></td>
	<% else %>
		<td> </td>	
	<% end %>
</tr>



<!--

<% if current_user.admin? %>
		<td> <%= link_to "view", edit_provider_path(provider_row), remote: true %> | <%= link_to "manage patients", manage_provider_patients_path(provider_row)%> | <%= link_to "delete", :controller=> "providers", :action => "remove", :id => "#{provider_row.id}" %> </td>
	<% else %>
		<td> </td>	
	<% end %>
-->
